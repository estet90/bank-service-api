CREATE TABLE CONTACT (
  CONTACT_ID INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1) NOT NULL);

CREATE TABLE APPLICATION (
  APPLICATION_ID INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1) NOT NULL,
  CONTACT_ID INT,
  PRODUCT_NAME VARCHAR(100) NOT NULL,
  DT_CREATED TIMESTAMP);

ALTER TABLE CONTACT ADD PRIMARY KEY (CONTACT_ID);

ALTER TABLE APPLICATION ADD PRIMARY KEY (APPLICATION_ID);

ALTER TABLE APPLICATION
  ADD FOREIGN KEY (CONTACT_ID)
REFERENCES CONTACT(CONTACT_ID);
